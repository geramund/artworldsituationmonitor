import ReviewQueue from "@/components/ReviewQueue";

export const metadata = {
  title: "Review Queue — Art World Situation Monitor",
};

export default function ReviewPage() {
  return <ReviewQueue />;
}
